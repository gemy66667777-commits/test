/* =============================================================
   إعدادات الاتصال بقاعدة البيانات (Supabase)
   -------------------------------------------------------------
   • اتركه فارغاً  → الموقع يشتغل من ملف data.js كما هو الآن،
                      وتعديلات الأدمن تبقى على متصفحه فقط.
   • املأ القيمتين → الموقع يقرأ ويكتب في قاعدة بيانات حقيقية،
                      وتعديلات الأدمن يراها كل الزوار فوراً.

   القيمتان من لوحة Supabase: Settings ← API
   ⚠️ ضع مفتاح anon public فقط. لا تضع service_role هنا إطلاقاً،
      لأن هذا الملف يقرأه أي زائر.
   ============================================================= */

const SUPABASE_URL      = '';   // مثال: https://abcdefgh.supabase.co
const SUPABASE_ANON_KEY = '';   // مفتاح anon public الطويل
