# ملاحظات المشروع

الريبو ده فيه مواقع لبراندات مختلفة، كل براند في فولدر لوحده.

## المواقع الموجودة

| الفولدر | البراند | المجال | الدومين |
|---|---|---|---|
| `alsadani/` | مول السعدني للمفروشات | مفروشات — طنطا | alsadani-mafroshat.vercel.app |
| `abo3omar/` | أبو عمر للأجهزة المنزلية | أجهزة منزلية — القاهرة | abo3omar.vercel.app |
| `forkknife/` | شوكة وسكينة | أدوات منزلية — دكرنس | forkknife.vercel.app |
| `hadaeq/` | حدائق الزهور للعطور | عطور نيش — الدمام 🇸🇦 | hadaeq-alzohour.vercel.app |
| `mrmedia/` | Mr.Media | تنظيم مناسبات وكوش أفراح | mrmedia-events.vercel.app |
| `elaj/` | مستشفى علاج | مستشفى — مدينة نصر | elaj-hospital.vercel.app |
| `drsalem/` | د. محمود سالم | عيادة عيون — الشيخ زايد | drsalem.vercel.app |
| `drmai/` | د. مي السادات | جراحة عامة ومناظير — زايد والمنيل | drmai.vercel.app |
| `vip/` | V.I.P | إكسسوارات | vip-website-three.vercel.app |
| `bahia/` | باهية | مكياج — السعودية | — |
| `shatha-almadina/` | شذا المدينة | — | — |
| الروت | الروقي للعطور | عطور — الكويت | — |

**منتجات جاهزة في `products/`:** `senter/` نظام متابعة طلبة السناتر · `jadwa/` مولّد دراسة الجدوى.

## قواعد ثابتة — متسألش عنها

1. **الدومين:** المستخدم بيرفع على Vercel بنفسه، والدومين بيبقى `<اسم-البراند>.vercel.app`.
   حطّه في `index.html` و `data.js` و `README.md` من الأول من غير ما تسأل.
   **⚠️ وقول له الاسم بالظبط في أول سطر من الرد وفي الـ README** — لأنه لو رفع باسم مختلف
   الثامبنيل بيقع، والتصليح بيحتاج رفع تاني. ولو ملوش لاب ساعتها، الحل من الموبايل:
   Vercel → Settings → Domains → Add ودومين مطابق للمكتوب في الملفات.
   فكّره يعمل **Scrape Again** في فيسبوك ديبَجر عشان الثامبنيل يتحدّث.

2. **التسليم:** بعد ما تخلص، ابعت **ملف ZIP** الملفات فيه في الجذر مباشرة (مش جوه فولدر)
   عشان يترفع على Vercel على طول. وارفع كمان على البرانش في جيت هب.

3. **اللغة:** كل الكلام مع المستخدم بالعربي المصري. مفيش إنجليزي.
   **بس نصوص الموقع نفسه تتكتب بلهجة بلد الزبون** — مصري للبراندات المصرية،
   وعربي فصيح/خليجي للسعودية والخليج (ومعاه العملة الصح: ج.م / ر.س).

4. **الأسعار:** لو مبعتش أسعار، حطّ تقديرية من السوق المصري وقوله صراحة إنها محتاجة مراجعة.
   نسب الخصم لازم تكون واقعية للمجال (مفروشات ٢٠–٣٠٪، أجهزة ٦–١٠٪ لأن هامشها ضعيف).
   **ولو صاحب البراند قال «من غير أسعار» — اقفل `showPrices` وخلاص.** الأسعار بتختفي
   من كروت المنتجات والباقات والقايمة كلها وبتتبدل بجملة `priceNote` القصيرة.
   وده كمان الأنسب لأي بيزنس خدمات (مناسبات، تصوير، مقاولات) لأن السعر بيختلف كل مرة.

5. **بيزنس خدمات مش منتجات:** زوّد في فورم الحجز حقول **تاريخ المناسبة ونوعها ومكانها** —
   أهم سؤال عند العميل هو «التاريخ ده متاح؟»، والرسالة اللي بتوصل على واتساب لازم تجاوب عليه.

6. **متسألش أسئلة كتير** — نفّذ بافتراضات معقولة وقول الافتراضات في الآخر.

## معمار المواقع (السعدني وأبو عمر)

الاتنين على نفس البنية — لو هتعمل براند جديد، انسخ `mrmedia/` وعدّل:

```
index.html              الموقع
admin.html              لوحة التحكم
assets/css/style.css    ستايل الموقع (متغيرات الألوان في :root)
assets/css/admin.css    ستايل اللوحة
assets/css/fonts.css    الخطوط (Noto Sans Arabic + Changa + Italiana محليًا)
assets/js/data.js       ← البيانات الأصلية (الوحيد اللي بيتغيّر لكل براند)
assets/js/store.js      طبقة التخزين (localStorage) — غيّر اسم المفتاح
assets/js/app.js        منطق الموقع + قايمة الطلب
assets/js/admin.js      منطق اللوحة
```

**مهم عند نسخ براند جديد:** غيّر اسم الـ seed (`ABO_SEED`) واسم الـ store (`AStore`)
ومفاتيح `localStorage` عشان مواقع مختلفة على نفس الدومين ما تتخبطش في بعض.

### مميزات لازم تفضل موجودة

- **العروض بالكميات:** `items: [{id, q}]` — عدد القطع والسعر قبل العرض بيتحسبوا لوحدهم.
  و`was` و`pieces` اختياريين للكتابة اليدوي، واللوحة بتحذّر لو المكتوب مختلف عن المحسوب.
- **منتجات من غير صور:** `img: ""` → رسمة بديلة مكتوب عليها «الصورة قريبًا».
- **بحث فوري** جنب فلاتر الأقسام.
- **قايمة طلب** بتفتح واتساب برسالة كاملة بالكميات والإجمالي (إشعار عند الإضافة، مش فتح الدرج).
- **لوحة تحكم** بتبويبات: نظرة عامة، المنتجات، العروض، الأقسام، المعرض، آراء العملاء،
  الفروع، الطلبات، الإعدادات. ورفع الصور من الجهاز مباشرة.
- **صورة مشاركة** `og-cover.jpg` مقاس ١٢٠٠×٦٣٠ مع وسوم `og:image:width/height/type`.
- **زرار الواتساب منوّر** بنبضة وهالة، وبتقف مع `prefers-reduced-motion`.

### تنضيف الصور
صور البراندات بتيجي عليها علامات مائية (QR سناب/إنستجرام، أرقام تليفون بانرات صفرا).
**شيلها قبل ما تحطها في الموقع** — بتوطّي الشكل جدًا. الطريقة: كاشف QR بـ numpy
(مربع تباينه عالي وبكسلاته شبه أبيض/أسود)، وبعدين ترقيع بعيّنة من منطقة جنبها + بلور،
أو قص الصورة لو الترقيع مش مقنع.

## الاختبار قبل التسليم

شغّل سيرفر محلي وافحص بـ Playwright (Chromium في `/opt/pw-browsers/chromium-1194/`):
صفر أخطاء جافاسكريبت · مفيش تمرير أفقي على ٣٩٠ بكسل · كل الصور بتحمّل ·
البحث والفلاتر والقايمة واللايت بوكس · روابط الواتساب · الدخول للوحة والتعديل
بيظهر في الموقع.

⚠️ **فخ معروف ١:** `padding` المختصر في عنصر عليه كلاس `.wrap` بيمسح الـ padding الجانبي
وبيطلّع المحتوى بره الشاشة على الموبايل. استخدم `padding-block` بدله.

⚠️ **فخ معروف ٣ (RTL):** `inset-inline-end` في `dir="rtl"` معناه **الشمال**، و`translateX`
مالوش علاقة بالاتجاه (بيزحلق لليمين دايمًا). فدرج بييجي من اليمين لازم يكون
`inset-inline-start:0` + `translateX(100%)`. العكس بيخلي الدرج ظاهر في نص الشاشة.
**وده مابيعملش تمرير أفقي**، يعني فحص الـ scrollWidth مش هيمسكه — لازم تقيس
`getBoundingClientRect()` وتبص على الصورة بعينك.

⚠️ **فخ معروف ٤:** متستخدمش كلاس قصير زي `.lg` لحاجتين مختلفين (اللوجو وحجم الزرار) —
`.lg svg{stroke:none}` بتاعة اللوجو هتخفي أيقونات كل زرار `.btn.lg`. سمّي المقاسات
`.big` أو `.btn--lg` وخلّي كلاسات الأنماط مميزة.

⚠️ **فخ معروف ٥:** `body{overflow-x:hidden}` لوحدها مش بتمنع `scrollWidth` من إنه يزيد
بسبب درج بره الشاشة. حط `html{overflow-x:clip}` كمان.

⚠️ **فخ معروف ٦ (عربي):** `line-height` أقل من ١٫٢٥ مع خط Changa في عناوين كبيرة
بيخلي السطور تتداخل. و`toLocaleString("ar-EG")` بيحط فاصلة آلاف في السنين (٢٬٠٢٦) —
استخدم تحويل يدوي للأرقام العربية.

⚠️ **فخ معروف ٧:** `Store.patch()` بيطلّق حدث `change` اللي بيعمل `render()` فورًا —
واللي بيصفّر أي متغيرات اختيار (اليوم/الساعة). **امسك القيم في متغيرات محلية قبل الحفظ**
وانت بتبني رسالة الواتساب، وإلا هتطلع `null`.

⚠️ **فخ معروف ٢:** زرار الواتساب العائم (`.wa-fab`) بيقعد فوق لينك لوحة التحكم اللي في
الفوتر على الموبايل، فمينفعش يتداس عليه. الحل موجود في كل المواقع: `footer{padding-block-end:96px}`
و `.fbot` عمودي ومتوسط واللينك زرار بحدود. **اختبر التداخل دايمًا** بمقارنة
`getBoundingClientRect()` بتاع الاتنين، ودوس على اللينك فعليًا في الاختبار.
